<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiling.user.procrelation.dao.PopProcTemplateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yiling.user.procrelation.entity.PopProcTemplateDO">
    <result column="id" property="id" />
        <result column="template_number" property="templateNumber" />
        <result column="template_name" property="templateName" />
        <result column="del_flag" property="delFlag" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
        <result column="update_user" property="updateUser" />
        <result column="update_time" property="updateTime" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        template_number, template_name, del_flag, create_user, create_time, update_user, update_time, remark
    </sql>

        <select id="queryAutoIncrement" parameterType="java.lang.String" resultType="java.lang.Long">
        SELECT
            auto_increment
        FROM
            information_schema.TABLES
        WHERE
            table_name = "pop_proc_template"
            <if test="dbName != null and dbName != ''">
                AND table_schema = #{dbName}
            </if>
        </select>

</mapper>
