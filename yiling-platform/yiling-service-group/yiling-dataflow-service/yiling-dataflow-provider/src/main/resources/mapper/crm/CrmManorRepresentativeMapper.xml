<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yiling.dataflow.crm.dao.CrmManorRepresentativeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yiling.dataflow.crm.entity.CrmManorRepresentativeDO">
        <result column="id" property="id"/>
        <result column="manor_id" property="manorId"/>
        <result column="representative_post_code" property="representativePostCode"/>
        <result column="del_flag" property="delFlag"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 通用查询结果列 -->

    <select id="pageList" resultType="com.yiling.dataflow.crm.dto.CrmManorRepresentativeDTO">
        SELECT a.id as manorId,
        a.manor_no as manorNo,
        a.`name` as name,
        a.update_time as updateTime,
        a.update_user as updateUser,
        b.representative_post_code as representativePostCode
        FROM crm_manor a
        LEFT JOIN crm_manor_representative b ON a.id = b.manor_id
        WHERE a.del_flag = 0 and a.agency_num>0
        <if test="request.manorNo !=null and request.manorNo!=''">and  a.manor_no=#{request.manorNo}</if>
        <if test="request.name !=null and request.name!=''">and a.name like CONCAT('%',#{request.name},'%')</if>
        <if test="request.dataScope !=null and request.dataScope==2">and b.representative_post_code is null</if>
        <if test="request.beginTime !=null">and a.update_time>=#{request.beginTime}</if>
        <if test="request.endTime !=null"><![CDATA[and  a.update_time<=#{request.endTime}]]></if>
        <if test="request.representativePostCode != null ">and  b.representative_post_code=#{request.representativePostCode}</if>
        order by a.create_time desc
    </select>
</mapper>
